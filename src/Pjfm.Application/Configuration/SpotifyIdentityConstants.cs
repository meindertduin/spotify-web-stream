namespace Pjfm.Application.Configuration
{
    public struct SpotifyIdentityConstants
    {
        public struct Claims
        {
            public const string SpStatus = nameof(SpStatus);
        }
        
        public struct Roles
        {
            public const string Auth = nameof(Auth);
        }
    }
}